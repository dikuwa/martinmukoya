"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useMemo, useState } from "react";
import toast from "react-hot-toast";
import { Plus, Trash2 } from "lucide-react";

type Source = { key:string; kind:"booking"|"lead"; id:string; label:string; site:string; name:string; email:string; phone:string; company:string; description:string };
type Invoice = { id:string; label:string; balance:number };
type Line = { description:string; category:string; quantity:string; unitPrice:string };
const input="h-10 rounded-xl border border-[color:var(--border-subtle)] bg-[color:var(--background)] px-3 text-sm font-normal outline-none focus:border-[color:var(--primary)]";

export function CreateDocumentPanel({sources,initialBooking,initialLead}:{sources:Source[];initialBooking?:string;initialLead?:string}){
  const router=useRouter(); const [sourceKey,setSourceKey]=useState(initialBooking?`booking:${initialBooking}`:initialLead?`lead:${initialLead}`:sources[0]?.key||""); const [type,setType]=useState("QUOTE"); const [lines,setLines]=useState<Line[]>([{description:"Project services",category:"service",quantity:"1",unitPrice:"0"}]); const [saving,setSaving]=useState(false);
  const source=useMemo(()=>sources.find(item=>item.key===sourceKey),[sources,sourceKey]);
  function updateLine(index:number,key:keyof Line,value:string){setLines(current=>current.map((line,i)=>i===index?{...line,[key]:value}:line))}
  async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(!source)return;setSaving(true);const form=new FormData(event.currentTarget);let bookingId=source.id;if(source.kind==="lead"){const conversion=await fetch("/api/admin/bookings/from-lead",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({leadId:source.id})});const converted=await conversion.json();if(!conversion.ok){setSaving(false);return toast.error(converted.error||"Lead could not be converted")};bookingId=converted.id}
    const body={bookingId,type,customerName:String(form.get("customerName")),customerEmail:String(form.get("customerEmail")||"")||null,customerPhone:String(form.get("customerPhone")||"")||null,customerCompany:String(form.get("customerCompany")||"")||null,customerAddress:String(form.get("customerAddress")||"")||null,validUntil:String(form.get("validUntil")||"")||null,dueDate:String(form.get("dueDate")||"")||null,discountAmount:String(form.get("discountAmount")||"0"),additionalCharges:String(form.get("additionalCharges")||"0"),taxRate:String(form.get("taxRate")||"0"),notes:String(form.get("notes")||"")||null,paymentTerms:String(form.get("paymentTerms")||"")||null,lines};
    const response=await fetch("/api/admin/documents",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const payload=await response.json();setSaving(false);if(!response.ok)return toast.error(payload.error||"Document could not be created");toast.success("Draft created");router.push(`/admin/documents/${payload.id}`);router.refresh()}
  return <form id="new-document" onSubmit={submit} className="grid gap-5 rounded-[18px] border border-[color:var(--border-subtle)] bg-[color:var(--surface)] p-5"><div><h2 className="font-display text-xl font-black">Create document</h2><p className="text-sm text-[color:var(--text-muted)]">Select a lead or booking, then set the actual scope and price.</p></div><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Source<select value={sourceKey} onChange={e=>setSourceKey(e.target.value)} className={input}>{sources.map(item=><option key={item.key} value={item.key}>{item.label} · {item.site}</option>)}</select></label><label className="grid gap-2 text-sm font-bold">Document type<select value={type} onChange={e=>setType(e.target.value)} className={input}><option value="QUOTE">Quotation</option><option value="INVOICE">Invoice</option></select></label><label className="grid gap-2 text-sm font-bold">Customer<input key={`${sourceKey}-name`} name="customerName" defaultValue={source?.name} required className={input}/></label><label className="grid gap-2 text-sm font-bold">Company<input key={`${sourceKey}-company`} name="customerCompany" defaultValue={source?.company} className={input}/></label><label className="grid gap-2 text-sm font-bold">Email<input key={`${sourceKey}-email`} name="customerEmail" type="email" defaultValue={source?.email} className={input}/></label><label className="grid gap-2 text-sm font-bold">Phone<input key={`${sourceKey}-phone`} name="customerPhone" defaultValue={source?.phone} className={input}/></label><label className="grid gap-2 text-sm font-bold">{type==="QUOTE"?"Valid until":"Due date"}<input name={type==="QUOTE"?"validUntil":"dueDate"} type="date" className={input}/></label><label className="grid gap-2 text-sm font-bold">Customer address<input name="customerAddress" className={input}/></label></div><div className="grid gap-3"><div className="flex items-center justify-between"><h3 className="font-bold">Line items</h3><button type="button" onClick={()=>setLines(current=>[...current,{description:"",category:"service",quantity:"1",unitPrice:"0"}])} className="inline-flex items-center gap-2 text-sm font-bold text-[color:var(--primary)]"><Plus size={15}/>Add item</button></div>{lines.map((line,index)=><div key={index} className="grid gap-2 rounded-xl bg-[color:var(--surface-soft)] p-3 md:grid-cols-[1fr_150px_90px_130px_36px]"><input aria-label={`Line ${index+1} description`} placeholder="Description" value={line.description} onChange={e=>updateLine(index,"description",e.target.value)} required className={input}/><input aria-label={`Line ${index+1} category`} placeholder="Category" value={line.category} onChange={e=>updateLine(index,"category",e.target.value)} className={input}/><input aria-label={`Line ${index+1} quantity`} type="number" min="0.001" step="0.001" value={line.quantity} onChange={e=>updateLine(index,"quantity",e.target.value)} required className={input}/><input aria-label={`Line ${index+1} unit price`} type="number" min="0" step="0.01" value={line.unitPrice} onChange={e=>updateLine(index,"unitPrice",e.target.value)} required className={input}/><button type="button" aria-label={`Remove line ${index+1}`} disabled={lines.length===1} onClick={()=>setLines(current=>current.filter((_,i)=>i!==index))} className="grid place-items-center text-[color:var(--destructive)] disabled:opacity-30"><Trash2 size={16}/></button></div>)}</div><div className="grid gap-4 md:grid-cols-3"><label className="grid gap-2 text-sm font-bold">Additional charges<input name="additionalCharges" type="number" min="0" step="0.01" defaultValue="0" className={input}/></label><label className="grid gap-2 text-sm font-bold">Discount<input name="discountAmount" type="number" min="0" step="0.01" defaultValue="0" className={input}/></label><label className="grid gap-2 text-sm font-bold">Tax %<input name="taxRate" type="number" min="0" step="0.01" defaultValue="0" className={input}/></label></div><div className="grid gap-4 md:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Notes<textarea name="notes" className={`${input} min-h-24 py-3`}/></label><label className="grid gap-2 text-sm font-bold">Payment terms<textarea name="paymentTerms" defaultValue="Payment due according to the agreed project schedule." className={`${input} min-h-24 py-3`}/></label></div><button disabled={saving||!source} className="rounded-xl bg-[color:var(--primary)] px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{saving?"Creating draft…":"Create draft"}</button></form>
}

export function PaymentPanel({invoices}:{invoices:Invoice[]}){const router=useRouter();const [invoiceId,setInvoiceId]=useState(invoices[0]?.id||"");const [saving,setSaving]=useState(false);const invoice=invoices.find(item=>item.id===invoiceId);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;setSaving(true);const data=Object.fromEntries(new FormData(form));const response=await fetch("/api/admin/payments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...data,invoiceId})});const payload=await response.json();setSaving(false);if(!response.ok)return toast.error(payload.error||"Payment could not be recorded");form.reset();toast.success("Payment recorded and receipt issued");router.refresh()}
return <form onSubmit={submit} className="grid gap-4 rounded-[18px] border border-[color:var(--border-subtle)] bg-[color:var(--surface)] p-5"><div><h2 className="font-display text-xl font-black">Record payment</h2><p className="text-sm text-[color:var(--text-muted)]">A receipt is issued automatically.</p></div>{invoices.length?<><label className="grid gap-2 text-sm font-bold">Invoice<select value={invoiceId} onChange={e=>setInvoiceId(e.target.value)} className={input}>{invoices.map(item=><option key={item.id} value={item.id}>{item.label}</option>)}</select></label><label className="grid gap-2 text-sm font-bold">Amount (N$)<input name="amount" required type="number" min="0.01" max={invoice?.balance} step="0.01" className={input}/></label><label className="grid gap-2 text-sm font-bold">Method<select name="method" className={input}><option value="CASH">Cash</option><option value="BANK_TRANSFER">Bank transfer</option><option value="CARD">Card</option><option value="OTHER">Other</option></select></label><label className="grid gap-2 text-sm font-bold">Reference<input name="reference" className={input}/></label><label className="grid gap-2 text-sm font-bold">Payment date<input name="paidAt" type="date" className={input}/></label><label className="grid gap-2 text-sm font-bold">Notes<textarea name="notes" className={`${input} min-h-20 py-3`}/></label><button disabled={saving} className="rounded-xl bg-[color:var(--primary)] px-5 py-3 text-sm font-bold text-white">{saving?"Recording…":"Record payment"}</button></>:<p className="rounded-xl bg-[color:var(--surface-soft)] p-4 text-sm text-[color:var(--text-muted)]">No outstanding issued invoices.</p>}</form>}

export function RegisterLink({href,primary,secondary,trailing}:{href:string;primary:string;secondary:string;trailing:string}){return <Link href={href} className="grid grid-cols-[1fr_auto] gap-2 border-b border-[color:var(--border-subtle)] px-4 py-3 text-sm last:border-0 hover:bg-[color:var(--surface-soft)]"><span><strong>{primary}</strong><small className="ml-2 text-[color:var(--text-muted)]">{secondary}</small></span><span className="font-semibold">{trailing}</span></Link>}
