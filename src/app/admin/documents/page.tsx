import { CreateDocumentPanel, PaymentPanel, RegisterLink } from "@/components/admin/finance-workspace";
import { PageHeader } from "@/components/ui/page-header"; import { Button } from "@/components/ui/button"; import Link from "next/link"; import { db } from "@/lib/db"; import { formatNad } from "@/lib/financial";
type Props={searchParams:Promise<{booking?:string}>};
export default async function DocumentsPage({searchParams}:Props){const params=await searchParams;const [bookings,leads,documents,payments]=await Promise.all([db.booking.findMany({where:{status:{in:["DRAFT","ACTIVE"]}},orderBy:{createdAt:"desc"},include:{site:true}}),db.lead.findMany({where:{financialBooking:null,status:{not:"ARCHIVED"},siteId:{not:null}},orderBy:{createdAt:"desc"},include:{site:true}}),db.financialDocument.findMany({orderBy:{createdAt:"desc"},take:100,include:{booking:true,site:true}}),db.payment.findMany({where:{reversedAt:null},orderBy:{paidAt:"desc"},take:100,include:{booking:true,invoice:true,receiptDocument:true,site:true}})]);
const sources=[...bookings.map(b=>({key:`booking:${b.id}`,kind:"booking" as const,id:b.id,label:`${b.number} · ${b.customerName}`,site:b.site.name,name:b.customerName,email:b.customerEmail||"",phone:b.customerPhone||"",company:b.company||"",description:b.projectDescription})),...leads.map(l=>({key:`lead:${l.id}`,kind:"lead" as const,id:l.id,label:`Lead · ${l.name}`,site:l.site?.name||"",name:l.name,email:l.email,phone:l.phone||"",company:l.company||"",description:l.projectGoal}))];
const invoices=documents.filter(d=>d.type==="INVOICE"&&["ISSUED","PARTIALLY_PAID"].includes(d.status)).map(d=>{const paid=payments.filter(p=>p.invoiceId===d.id).reduce((s,p)=>s+Number(p.amount),0);return{id:d.id,label:`${d.number} · ${d.customerName} · ${formatNad(Number(d.total)-paid)} due`,balance:Number(d.total)-paid}});
return <div className="grid gap-8"><PageHeader title="Documents and payments" description="Create FlexTech Media quotations and invoices, record payments, and issue receipts." actions={<Button asChild variant="secondary"><Link href="/admin/documents/settings">Financial identity</Link></Button>}/><div className="grid items-start gap-5 xl:grid-cols-[1fr_360px]"><CreateDocumentPanel sources={sources} initialBooking={params.booking}/><PaymentPanel invoices={invoices}/></div><div className="grid gap-5 xl:grid-cols-2"><section className="overflow-hidden rounded-[18px] border border-[color:var(--border-subtle)] bg-[color:var(--surface)]"><h2 className="border-b border-[color:var(--border-subtle)] p-4 font-display text-xl font-black">Document register</h2>{documents.map(d=><RegisterLink key={d.id} href={`/admin/documents/${d.id}`} primary={d.number||"Draft"} secondary={`${d.customerName} · ${d.booking.number} · ${d.type} · ${d.status}`} trailing={formatNad(String(d.total))}/>) }{!documents.length?<p className="p-6 text-sm text-[color:var(--text-muted)]">No documents yet.</p>:null}</section><section className="overflow-hidden rounded-[18px] border border-[color:var(--border-subtle)] bg-[color:var(--surface)]"><h2 className="border-b border-[color:var(--border-subtle)] p-4 font-display text-xl font-black">Payment register</h2>{payments.map(p=><RegisterLink key={p.id} href={p.receiptDocument?`/admin/documents/${p.receiptDocument.id}`:`/admin/bookings/${p.bookingId}`} primary={p.number} secondary={`${p.booking.customerName} · ${p.method} · ${p.invoice?.number||"Unallocated"}`} trailing={formatNad(String(p.amount))}/>) }{!payments.length?<p className="p-6 text-sm text-[color:var(--text-muted)]">No payments yet.</p>:null}</section></div></div>}
