import{notFound}from"next/navigation";import{db}from"@/lib/db";import{PageHeader}from"@/components/ui/page-header";import{BookingEditForm}from"@/components/admin/booking-edit-form";
export default async function EditBooking({params}:{params:Promise<{id:string}>}){const[booking,sites]=await Promise.all([db.booking.findUnique({where:{id:(await params).id}}),db.site.findMany({orderBy:{name:"asc"},select:{id:true,name:true}})]);if(!booking)notFound();return <div className="grid gap-8"><PageHeader title={`Edit ${booking.number}`} description="Update customer and project details without changing issued document snapshots."/><BookingEditForm booking={booking} sites={sites}/></div>}
