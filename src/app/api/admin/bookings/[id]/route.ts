import{ok,parseJson,serverError,validationError}from"@/lib/api";import{requireAdmin}from"@/lib/auth-guard";import{bookingCreateSchema}from"@/lib/validation/finance";import{db}from"@/lib/db";import{z}from"zod";type Context={params:Promise<{id:string}>};
export async function PATCH(request:Request,context:Context){try{const{error}=await requireAdmin();if(error)return error;const input=await parseJson(request,bookingCreateSchema);const id=(await context.params).id;const existing=await db.booking.findUnique({where:{id}});if(!existing)return Response.json({error:"Booking not found"},{status:404});return ok(await db.booking.update({where:{id},data:{siteId:input.siteId,customerName:input.customerName,customerEmail:input.customerEmail,customerPhone:input.customerPhone,company:input.company,projectDescription:input.projectDescription,budgetRange:input.budgetRange}}))}catch(error){if(error instanceof z.ZodError)return validationError(error);return serverError(error)}}
