import { renderToBuffer } from "@react-pdf/renderer"; import { requireAdmin } from "@/lib/auth-guard"; import { db } from "@/lib/db"; import { FinancialPdf } from "@/lib/pdf/financial-document";
type Context={params:Promise<{id:string}>};export const runtime="nodejs";
export async function GET(request:Request,context:Context){const {error}=await requireAdmin();if(error)return error;const document=await db.financialDocument.findUnique({where:{id:(await context.params).id},include:{booking:true,lineItems:{orderBy:{sortOrder:"asc"}},payments:true,receiptForPayment:true}});if(!document)return Response.json({error:"Document not found"},{status:404});const baseUrl=new URL(request.url).origin;const buffer=await renderToBuffer(<FinancialPdf document={document} baseUrl={baseUrl}/>);const filename=`${(document.number||"draft").replace(/[^a-z0-9-]/gi,"_")}.pdf`;return new Response(new Uint8Array(buffer),{headers:{"content-type":"application/pdf","content-disposition":`attachment; filename="${filename}"`,"cache-control":"private, no-store"}})}
