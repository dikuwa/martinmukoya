/**
 * Unit tests for the Activity Cleanup feature.
 *
 * Installation required: pnpm add -D vitest
 * Then run: npx vitest run tests/activity-cleanup.test.ts
 *
 * For now, use the standalone validation script instead:
 *   npx tsx tests/validate-cleanup.ts
 */
